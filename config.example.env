# ===================== MOONSTONE IMPORTER CONFIGURATION =====================
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to version control!

# ===================== NOTION CONFIGURATION =====================
# Your Notion integration API key
# Get this from: https://www.notion.so/my-integrations
NOTION_API_KEY=secret_your_notion_api_key_here

# The ID of your Notion database where data will be imported
# Extract from the database URL: https://notion.so/your-workspace/DATABASE_ID?v=...
NOTION_DATABASE_ID=your_database_id_here

# ===================== GOOGLE SHEETS CONFIGURATION =====================
# The ID of your Google Spreadsheet containing form responses
# Extract from the sheet URL: https://docs.google.com/spreadsheets/d/SHEET_ID/edit
GOOGLE_SHEET_ID=your_google_sheet_id_here

# Google Service Account credentials as JSON string
# Create a service account at: https://console.cloud.google.com/iam-admin/serviceaccounts
# Download the JSON key file and paste its contents here as a single line
GAPI_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"your-project","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"your-service-account@your-project.iam.gserviceaccount.com","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"..."}

# ===================== OPTIONAL CONFIGURATION =====================
# Set to 'development' for detailed logging and TypeScript direct execution
NODE_ENV=production

# ===================== SETUP INSTRUCTIONS =====================
# 1. Copy this file to .env: cp config.example.env .env
# 2. Fill in all the values above with your actual credentials
# 3. Make sure your Google service account has access to the spreadsheet
# 4. Make sure your Notion integration has access to the database
# 5. Test the configuration with: npm run dev

# ===================== SECURITY NOTES =====================
# - Never commit the .env file to version control
# - Keep your API keys secure and rotate them regularly  
# - Use different credentials for development and production
# - Limit service account permissions to only what's needed
